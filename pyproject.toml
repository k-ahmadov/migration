[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mypackages"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["mysolvers*", "mypackages*"]

[tool.ruff.lint]
ignore = ["F401"]
