[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mysolvers"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["mysolvers*"]

[tool.ruff.lint]
ignore = ["F401"]
